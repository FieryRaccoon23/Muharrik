{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug Muharrik (LLDB)",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/build/app",
      "args": [],
      "cwd": "${workspaceFolder}"
    }
  ]
}
